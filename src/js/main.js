function getValues (){
    var result = [];
    var i = 0;
do {
   var result[i] = +prompt ('Введите число 1'); 
   i++
   var isProcess = confirm ('Продолжить?')
} while (isProcess);
for (var m=0){}
alert ('Результат: ' + result);
}
